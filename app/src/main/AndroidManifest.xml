<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cegepapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CegepApplication">
        <activity android:name=".ViewStudents">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".forgotPasword" />
        <activity android:name=".ui.login.TeacherRegistration" />
        <activity
            android:name=".ui.login.RegisterActivity"
            android:label="@string/title_activity_register" ></activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".UserHomeActivity" />
        <activity android:name=".AdminHomeActivity" />
        <activity android:name=".AdminLoginActivity" />
        <activity android:name=".TeacherLoginActivity" />
        <activity android:name=".StudentLoginActivity" />
        <activity android:name=".UserSelectActivity" />
        <activity android:name=".SplashScreenActivity">

        </activity>
    </application>

</manifest>